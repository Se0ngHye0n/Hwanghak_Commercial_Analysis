pandas==2.3.3
numpy==2.3.5
scikit-learn==1.7.2
matplotlib==3.10.6
seaborn==0.13.2
jupyter==1.1.1

# geopandas는 01_data_understanding.ipynb에서 GIS(commercial_area) shapefile을 확인할 때만
# 선택적으로 사용된다(HAS_GEOPANDAS 가드 처리). 설치되어 있지 않아도 01~08 노트북 실행에는
# 영향을 주지 않으므로 기본 설치 목록에는 포함하지 않았다. 필요하면 별도로 설치한다.
# geopandas
